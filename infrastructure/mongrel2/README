Using Mongrel2 for Hastur REST Servers: A Love Story

First, load your config:

  $ ./load_config.sh

You'll need to set up mongrel2 with root permissions:

  $ sudo ./set_permissions.sh

And then you'll run procer to run and manage all the servers:

  $ ./run_servers.sh
